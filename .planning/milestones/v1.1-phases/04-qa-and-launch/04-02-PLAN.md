---
phase: 04-qa-and-launch
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "PageSpeed Insights mobile audit on https://conjurestudio.ai shows LCP ≤ 2.5s and identifies the hero image as the LCP element"
    - "Chrome DevTools Network tab on https://conjurestudio.ai shows all screenshot assets (hero + 6 feature images) as image/webp — no PNG or JPEG in production"
    - "Physical iPhone Safari renders https://conjurestudio.ai with no horizontal scroll, all CTAs fully visible and tappable, no layout breakage on scroll"
    - "Banned-word grep across all .tsx and .ts files in conjure-landing-page/src/ (excluding test files) returns zero matches"
    - "Submitting the waitlist form on https://conjurestudio.ai shows the success state — the live CORS+API path is confirmed end-to-end"
  artifacts:
    - path: "conjure-landing-page/src/components/sections/HeroSection.tsx"
      provides: "Hero image with priority prop (LCP optimization)"
      contains: "priority"
    - path: "conjure-landing-page/public/hero-screenshot.webp"
      provides: "WebP hero asset"
  key_links:
    - from: "HeroSection.tsx next/image"
      to: "public/hero-screenshot.webp"
      via: "priority prop + next/image optimization"
      pattern: "priority"
    - from: "WaitlistSection form submit"
      to: "conjurestudio.app/api/waitlist"
      via: "cross-origin fetch from https://conjurestudio.ai"
      pattern: "fetch.*conjurestudio.app/api/waitlist"
---

<objective>
Run the full pre-launch QA checklist against the deployed production URL to confirm all three PERF requirements pass before the page is declared launch-ready.

Purpose: The landing page is live at https://conjurestudio.ai. This plan verifies that performance (LCP ≤ 2.5s), asset format (WebP only), mobile rendering (physical iOS Safari), and copy compliance (zero banned words) all pass. It also confirms the waitlist form works end-to-end from production now that CORS is live (Plan 01).

Output: All five QA checks confirmed pass. Phase 4 complete — page is launch-ready.
</objective>

<execution_context>
@/Users/christopheraumen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopheraumen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-qa-and-launch/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Banned-word grep and test suite baseline</name>
  <files>(no files modified — verification only)</files>
  <action>
Run the banned-word grep against all .tsx and .ts files in conjure-landing-page/src/, excluding test files to avoid false positives from content.test.ts (which defines the banned word list as test data):

  grep -rn \
    "AI-powered\|platform\|solution\|leverage\|seamless\|intuitive\|workflow automation\|generative AI\|storyboard software\|asset management\|collaboration hub\|template\|streamline" \
    /Users/christopheraumen/Documents/Conjure-Landing-Page/conjure-landing-page/src/ \
    --include="*.tsx" --include="*.ts" \
    --exclude="*.test.*" --exclude="*.spec.*"

Expected: zero output (no matches). If any matches appear, note the file and line — those must be fixed before launch.

Then confirm the test suite is green:
  cd /Users/christopheraumen/Documents/Conjure-Landing-Page/conjure-landing-page && npx vitest run 2>&1 | tail -10

Expected: all 77 tests passing.

These two automated checks establish the baseline. If either fails, fix before proceeding to the manual checkpoint.
  </action>
  <verify>
    <automated>grep -rn "AI-powered\|platform\|solution\|leverage\|seamless\|intuitive\|workflow automation\|generative AI\|storyboard software\|asset management\|collaboration hub\|template\|streamline" /Users/christopheraumen/Documents/Conjure-Landing-Page/conjure-landing-page/src/ --include="*.tsx" --include="*.ts" --exclude="*.test.*" --exclude="*.spec.*" ; echo "Exit code: $?"</automated>
  </verify>
  <done>Grep returns zero matches (no output before the exit code line). Vitest shows 77 tests passing. Both automated baselines confirmed clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual QA checklist — Lighthouse, WebP, physical device, waitlist form</name>
  <files>(no files modified — manual verification against production URL)</files>
  <action>Work through all four manual checks below in order. Record pass/fail for the SUMMARY. These cannot be automated — they verify live production rendering, browser DevTools data, physical hardware, and an external API.</action>
  <verify>
    <automated>HUMAN-VERIFY — results provided via resume-signal after manual checks complete</automated>
  </verify>
  <done>All four checks pass: LCP ≤ 2.5s with hero as LCP element, all screenshot assets are image/webp, physical iPhone shows no layout breakage, waitlist form shows success state on submit.</done>
  <what-built>The production landing page at https://conjurestudio.ai, with CORS now live on the waitlist API (confirmed in Plan 01).</what-built>
  <how-to-verify>
Work through all four checks in order. Record pass/fail and any values for the SUMMARY.

---

CHECK 1: Lighthouse LCP (PERF-01)
1. Open https://pagespeed.web.dev in a browser
2. Paste https://conjurestudio.ai into the URL field
3. Run the MOBILE audit (not desktop)
4. In the results, find the "Largest Contentful Paint" metric — record the value
5. Confirm the LCP element is identified as the hero image (hero-screenshot.webp), not headline text
PASS: LCP ≤ 2.5s AND LCP element is the hero image
FAIL: LCP > 2.5s OR LCP element is something else (investigate TTFB, image size)

---

CHECK 2: WebP assets in Network tab (PERF-02)
1. Open https://conjurestudio.ai in Chrome
2. Open DevTools (F12) → Network tab → reload the page
3. Filter by "Img" type
4. Inspect each screenshot asset request (hero-screenshot, feature-*.webp)
5. Confirm every screenshot shows Content-Type: image/webp in response headers
6. Note: PNG files (Conjure-*.png) exist in /public but should NOT appear as network requests — they are source originals not referenced in components
PASS: All production screenshot assets return image/webp
FAIL: Any screenshot returns image/png or image/jpeg

---

CHECK 3: Physical iPhone Safari (PERF-03)
1. Open https://conjurestudio.ai on a physical iPhone in Safari
2. Swipe left/right — the page must not scroll horizontally
3. Scroll through the full page top to bottom — no sections clipped, no layout breakage
4. Tap each CTA: "Start free — no credit card" in hero, pricing tier CTAs — all must be fully visible and tappable
5. Bonus sanity: open /admin/login on iPhone — confirm it renders (not broken)
PASS: No horizontal scroll, no clipped CTAs, no layout breakage
FAIL: Any horizontal scroll, any CTA cut off, any section overflowing

---

CHECK 4: Waitlist form end-to-end on production (PERF-03 + CORS confirmation)
1. Go to https://conjurestudio.ai (must be production URL — localhost will fail)
2. Scroll to the waitlist form
3. Enter a test email (e.g., launch-qa-test@example.com) and optionally a name
4. Submit the form
5. Confirm the success state is shown (not an error state)
PASS: Form shows success state after submit
FAIL: Form shows error state or browser console shows CORS error

---

Record the LCP value and pass/fail for each check in your resume signal.
  </how-to-verify>
  <resume-signal>Provide results in this format:
- LCP: [value]s — [PASS/FAIL] — LCP element: [hero image / other]
- WebP: [PASS/FAIL]
- Physical iPhone: [PASS/FAIL] — notes if any
- Waitlist form: [PASS/FAIL]

If any check fails, describe the issue and we will diagnose before marking launch-ready.</resume-signal>
</task>

</tasks>

<verification>
Phase is complete when:
1. Banned-word grep: zero matches across all .tsx/.ts excluding test files
2. Test suite: 77 tests passing (npx vitest run)
3. Lighthouse mobile LCP ≤ 2.5s, hero image identified as LCP element
4. Network tab: all production screenshot assets return image/webp
5. Physical iPhone Safari: no horizontal scroll, all CTAs reachable, no layout breakage
6. Waitlist form on conjurestudio.ai: shows success state on submit
</verification>

<success_criteria>
- PERF-01: LCP ≤ 2.5s on mobile, hero-screenshot.webp is the LCP element
- PERF-02: All screenshot assets served as image/webp in production (no PNG/JPEG)
- PERF-03: Physical iOS Safari — no horizontal scroll, all CTAs tappable, waitlist form submits successfully from production domain
- COPY-01 confirmed: zero banned words in non-test source files
- Test suite: 77/77 tests passing
- Phase 4 complete — landing page is launch-ready
</success_criteria>

<output>
After completion, create `.planning/phases/04-qa-and-launch/04-02-SUMMARY.md` documenting:
- QA results for each of the five checks (pass/fail + values)
- LCP value recorded from PageSpeed Insights
- Any issues found and how resolved
- Launch-readiness declaration (all PERF requirements met)
</output>
