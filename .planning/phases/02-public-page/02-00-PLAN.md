---
phase: 02-public-page
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - conjure-landing-page/src/app/globals.css
  - conjure-landing-page/src/components/sections/__tests__/HeroSection.test.tsx
  - conjure-landing-page/src/components/sections/__tests__/PricingSection.test.tsx
  - conjure-landing-page/src/components/sections/__tests__/WaitlistSection.test.tsx
  - conjure-landing-page/src/components/sections/__tests__/SocialProofAndFAQ.test.tsx
  - conjure-landing-page/src/lib/content.test.ts
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HOW-01
  - HOW-02
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-05
  - FEAT-06
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-05
  - PRICE-06
  - WAIT-01
  - WAIT-02
  - WAIT-03
  - WAIT-04
  - WAIT-05
  - WAIT-06
  - WAIT-07
  - SOCIAL-01
  - SOCIAL-02
  - FAQ-01
  - FAQ-02
  - FAQ-03
  - COPY-01
  - COPY-02

must_haves:
  truths:
    - "All Wave 1+ verify commands have a test file that currently fails (RED)"
    - ".glass-surface utility class exists in globals.css and applies backdrop-filter"
    - ".fade-in-section and .fade-in-section.visible classes exist in globals.css"
    - "Banned-word test in content.test.ts fails until all copy is confirmed clean"
  artifacts:
    - path: "conjure-landing-page/src/app/globals.css"
      provides: ".glass-surface, .fade-in-section, .fade-in-section.visible utility classes"
      contains: ".glass-surface"
    - path: "conjure-landing-page/src/components/sections/__tests__/HeroSection.test.tsx"
      provides: "RED stubs for HERO-01,02,03,04"
    - path: "conjure-landing-page/src/components/sections/__tests__/PricingSection.test.tsx"
      provides: "RED stubs for PRICE-01,02,03,05,06"
    - path: "conjure-landing-page/src/components/sections/__tests__/WaitlistSection.test.tsx"
      provides: "RED stubs for WAIT-01 through WAIT-07"
    - path: "conjure-landing-page/src/components/sections/__tests__/SocialProofAndFAQ.test.tsx"
      provides: "RED stubs for SOCIAL-01,02 and FAQ-01,02,03"
    - path: "conjure-landing-page/src/lib/content.test.ts"
      provides: "Banned-word assertions for COPY-01,02"
  key_links:
    - from: "globals.css"
      to: "all glass card components"
      via: ".glass-surface class"
      pattern: "glass-surface"
    - from: "test stubs"
      to: "section component files"
      via: "import paths referenced in tests"
      pattern: "from.*sections/"
---

<objective>
Lay the Wave 0 foundation: CSS utility classes that all section components will reference, and RED test stubs that gate every subsequent wave's automated verification.

Purpose: Every plan in Waves 1-3 has an `<automated>` verify command. Those commands require test files to exist before the implementation is written. Creating them RED now enforces TDD discipline and prevents silent verification gaps.

Output: `.glass-surface` and `.fade-in-section` utility classes in `globals.css`; five test files with failing assertions covering HERO, PRICE, WAIT, SOCIAL, FAQ, and copy compliance.
</objective>

<execution_context>
@/Users/christopheraumen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopheraumen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-page/02-CONTEXT.md
@.planning/phases/02-public-page/02-RESEARCH.md
@.planning/phases/02-public-page/02-VALIDATION.md
@conjure-landing-page/src/app/globals.css
@conjure-landing-page/src/lib/content.ts
@conjure-landing-page/src/lib/content.test.ts
@conjure-landing-page/vitest.config.ts

<interfaces>
<!-- Key existing tokens and test patterns. Executors use these directly. -->

From conjure-landing-page/src/app/globals.css (existing CSS vars in :root):
```css
--glass-bg:     oklch(0.12 0 0 / 0.3);
--glass-border: oklch(0.98 0 0 / 0.1);
--glass-blur:   10px;
--glow-hover:   0 0 20px rgba(168, 250, 138, 0.3);
```

From conjure-landing-page/src/lib/content.ts (exports used in tests):
```typescript
export const HERO: { HEADLINE, SUBHEAD, CTA_PRIMARY, CTA_URL }
export const HOW_IT_WORKS: { STEPS: Array<{number, TITLE, BODY}> }
export const FEATURES: { COMPONENT_ASSEMBLY, CHARACTER_EXTRACTION, CAMERA_PRESETS, POSE_SHEETS, SLIDES_EXPORT }
export const PRICING: { TIERS: Array<{id,name,monthlyPrice,annualPrice,annualMonthly,credits,projects,seats}>, TRIAL_CTA, TRIAL_URL, TRIAL_DURATION, TRIAL_NOTE, BILLING_NOTE }
export const WAITLIST: { HEADING, SUBHEAD, SUBMIT_LABEL, NAME_PLACEHOLDER, EMAIL_PLACEHOLDER, SUCCESS_MESSAGE, ERROR_MESSAGE, ENDPOINT }
export const FAQ: { ITEMS: Array<{question, answer}> }
export const SOCIAL_PROOF: { SECTION_HEADING, TESTIMONIAL: null }
```

Test pattern from conjure-landing-page/src/lib/content.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
// dynamic imports used for isolation: const { X } = await import('./module')
```

vitest.config.ts: jsdom environment, globals: true, include: 'src/**/*.test.{ts,tsx}', @/ alias → src/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .glass-surface and .fade-in-section utility classes to globals.css</name>
  <files>conjure-landing-page/src/app/globals.css</files>
  <action>
Append two utility class blocks to the bottom of `globals.css` (after the existing `:root` glow effects block and `body` block). Do NOT modify any existing content.

Block 1 — glass surface utility (used by all card components):
```css
/* ─── Glass Surface Utility ──────────────────────────────────────────── */
/* Apply .glass-surface to any card or surface needing the glass effect  */
.glass-surface {
  background-color: var(--glass-bg);
  backdrop-filter: blur(var(--glass-blur));
  -webkit-backdrop-filter: blur(var(--glass-blur));
  border: 1px solid var(--glass-border);
  transition: box-shadow 200ms ease;
}
.glass-surface:hover {
  box-shadow: var(--glow-hover);
}
```

Block 2 — section fade-in on scroll (used with useFadeIn hook in Wave 3):
```css
/* ─── Section Fade-In ────────────────────────────────────────────────── */
/* Initial state: invisible and shifted down. JS adds .visible class.    */
.fade-in-section {
  opacity: 0;
  transform: translateY(1rem);
  transition: opacity 0.5s ease, transform 0.5s ease;
}
.fade-in-section.visible {
  opacity: 1;
  transform: translateY(0);
}
```

Notes:
- Uses `var(--glass-bg)` and `var(--glass-blur)` and `var(--glass-border)` already defined in `:root` block — do NOT redefine those vars
- Uses `var(--glow-hover)` already defined in the glow `:root` block — do NOT redefine
- `-webkit-backdrop-filter` is required for Safari support (see RESEARCH.md pitfall 5)
- `transition: box-shadow 200ms ease` prevents jarring instantaneous glow (see RESEARCH.md anti-patterns)
  </action>
  <verify>
    <automated>cd /Users/christopheraumen/Documents/Conjure-Landing-Page/conjure-landing-page && grep -c "glass-surface" src/app/globals.css && grep -c "fade-in-section" src/app/globals.css</automated>
  </verify>
  <done>globals.css contains `.glass-surface`, `.glass-surface:hover`, `.fade-in-section`, and `.fade-in-section.visible` classes. The two grep commands each return a number ≥ 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for all section components and extend content.test.ts</name>
  <files>
    conjure-landing-page/src/components/sections/__tests__/HeroSection.test.tsx,
    conjure-landing-page/src/components/sections/__tests__/PricingSection.test.tsx,
    conjure-landing-page/src/components/sections/__tests__/WaitlistSection.test.tsx,
    conjure-landing-page/src/components/sections/__tests__/SocialProofAndFAQ.test.tsx,
    conjure-landing-page/src/lib/content.test.ts
  </files>
  <behavior>
    All five files below must exist and all new tests must FAIL (RED) when run — the components they import do not exist yet. Existing content.test.ts tests must continue to pass GREEN.

    HeroSection.test.tsx behaviors (RED):
    - Renders HERO.HEADLINE text
    - Renders HERO.SUBHEAD text
    - CTA link has href === HERO.CTA_URL and text === HERO.CTA_PRIMARY
    - Contains HTML comment <!-- HERO_SCREENSHOT_REQUIRED --> or an img/Image element when screenshot exists
    - posthog.capture called with ('cta_clicked', { cta_label: 'Start free — no credit card', section: 'hero' }) on CTA click

    PricingSection.test.tsx behaviors (RED):
    - Renders all 4 tier names: Scout, Director, Producer, Studio
    - Default (monthly) view shows tier.monthlyPrice for each tier
    - After clicking annual toggle, shows tier.annualMonthly for each tier
    - Director tier card has 'Most popular' text
    - Tier CTA hrefs match checkoutUrls prop passed in
    - posthog.capture called with ('pricing_tier_viewed') when section enters viewport (mock IntersectionObserver)
    - posthog.capture called with ('cta_clicked', { section: 'pricing' }) on tier CTA click

    WaitlistSection.test.tsx behaviors (RED):
    - Renders email input (required) and name input (not required)
    - Submit button has text matching WAITLIST.SUBMIT_LABEL
    - Submit button is disabled while formState === 'submitting'
    - On 200 response: shows WAITLIST.SUCCESS_MESSAGE
    - On 4xx response: shows WAITLIST.ERROR_MESSAGE
    - On 200: posthog.capture called with ('waitlist_form_submitted', { email_domain: 'example.com', has_name: false })
    - On network error: posthog.capture called with ('waitlist_form_error', { error_type: 'network' })
    - fetch called with WAITLIST.ENDPOINT as URL and correct body

    SocialProofAndFAQ.test.tsx behaviors (RED):
    - SocialProofSection renders SOCIAL_PROOF.SECTION_HEADING
    - SocialProofSection contains the string 'TESTIMONIAL_REQUIRED' somewhere in rendered output
    - FAQSection renders all 3 FAQ.ITEMS questions
    - FAQSection renders answers (or summary/details elements)

    content.test.ts additions (should pass GREEN once content.ts is confirmed clean, but write as assertions now):
    - None of the HERO, HOW_IT_WORKS, FEATURES, PRICING, WAITLIST, FAQ, SOCIAL_PROOF strings contain any of the banned words: 'AI-powered', 'platform', 'solution', 'leverage', 'seamless', 'intuitive', 'workflow automation', 'generative AI', 'storyboard software', 'asset management', 'collaboration hub', 'template', 'streamline'
  </behavior>
  <action>
Create the directory `conjure-landing-page/src/components/sections/__tests__/` and write four new test files. Also extend the existing `content.test.ts`.

**File: src/components/sections/__tests__/HeroSection.test.tsx**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { HERO } from '@/lib/content'

// Mock posthog-js
vi.mock('posthog-js', () => ({
  default: { capture: vi.fn() },
}))

// Import posthog after mock
import posthog from 'posthog-js'

// Import component — does not exist yet (RED)
import { HeroSection } from '@/components/sections/HeroSection'

beforeEach(() => {
  vi.clearAllMocks()
})

describe('HeroSection', () => {
  it('renders HERO.HEADLINE', () => {
    render(<HeroSection />)
    expect(screen.getByText(HERO.HEADLINE)).toBeInTheDocument()
  })

  it('renders HERO.SUBHEAD', () => {
    render(<HeroSection />)
    expect(screen.getByText(HERO.SUBHEAD)).toBeInTheDocument()
  })

  it('CTA link has correct href and label', () => {
    render(<HeroSection />)
    const cta = screen.getByRole('link', { name: HERO.CTA_PRIMARY })
    expect(cta).toHaveAttribute('href', HERO.CTA_URL)
  })

  it('fires posthog cta_clicked on CTA click', () => {
    render(<HeroSection />)
    const cta = screen.getByRole('link', { name: HERO.CTA_PRIMARY })
    fireEvent.click(cta)
    expect(posthog.capture).toHaveBeenCalledWith('cta_clicked', {
      cta_label: HERO.CTA_PRIMARY,
      section: 'hero',
    })
  })

  it('contains HERO_SCREENSHOT_REQUIRED placeholder or screenshot image', () => {
    const { container } = render(<HeroSection />)
    const hasComment = container.innerHTML.includes('HERO_SCREENSHOT_REQUIRED')
    const hasImg = container.querySelector('img') !== null
    expect(hasComment || hasImg).toBe(true)
  })
})
```

**File: src/components/sections/__tests__/PricingSection.test.tsx**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { PRICING } from '@/lib/content'

vi.mock('posthog-js', () => ({
  default: { capture: vi.fn() },
}))

import posthog from 'posthog-js'
import { PricingSection } from '@/components/sections/PricingSection'

// Mock IntersectionObserver (jsdom does not implement it)
const mockObserver = { observe: vi.fn(), disconnect: vi.fn(), unobserve: vi.fn() }
vi.stubGlobal('IntersectionObserver', vi.fn(() => mockObserver))

const mockCheckoutUrls = {
  scout:    'https://example.com/scout',
  director: 'https://example.com/director',
  producer: 'https://example.com/producer',
  studio:   'https://example.com/studio',
}

beforeEach(() => {
  vi.clearAllMocks()
})

describe('PricingSection', () => {
  it('renders all 4 tier names', () => {
    render(<PricingSection checkoutUrls={mockCheckoutUrls} />)
    expect(screen.getByText('Scout')).toBeInTheDocument()
    expect(screen.getByText('Director')).toBeInTheDocument()
    expect(screen.getByText('Producer')).toBeInTheDocument()
    expect(screen.getByText('Studio')).toBeInTheDocument()
  })

  it('shows monthly prices by default', () => {
    render(<PricingSection checkoutUrls={mockCheckoutUrls} />)
    expect(screen.getByText(/\$39/)).toBeInTheDocument()
    expect(screen.getByText(/\$59/)).toBeInTheDocument()
  })

  it('shows annual monthly prices after clicking annual toggle', () => {
    render(<PricingSection checkoutUrls={mockCheckoutUrls} />)
    const toggle = screen.getByRole('button', { name: /annual/i })
    fireEvent.click(toggle)
    expect(screen.getByText(/\$32/)).toBeInTheDocument()
    expect(screen.getByText(/\$49/)).toBeInTheDocument()
  })

  it('Director tier has Most popular badge', () => {
    render(<PricingSection checkoutUrls={mockCheckoutUrls} />)
    expect(screen.getByText(/most popular/i)).toBeInTheDocument()
  })

  it('tier CTA hrefs match checkoutUrls prop', () => {
    render(<PricingSection checkoutUrls={mockCheckoutUrls} />)
    const links = screen.getAllByRole('link')
    const hrefs = links.map((l) => l.getAttribute('href'))
    expect(hrefs).toContain('https://example.com/director')
  })

  it('fires cta_clicked on tier CTA click', () => {
    render(<PricingSection checkoutUrls={mockCheckoutUrls} />)
    const directorLink = screen.getByRole('link', { name: /start.*director/i })
    fireEvent.click(directorLink)
    expect(posthog.capture).toHaveBeenCalledWith('cta_clicked', expect.objectContaining({ section: 'pricing' }))
  })
})
```

**File: src/components/sections/__tests__/WaitlistSection.test.tsx**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { WAITLIST } from '@/lib/content'

vi.mock('posthog-js', () => ({
  default: { capture: vi.fn() },
}))

import posthog from 'posthog-js'
import { WaitlistSection } from '@/components/sections/WaitlistSection'

beforeEach(() => {
  vi.clearAllMocks()
  vi.restoreAllMocks()
})

describe('WaitlistSection — form fields', () => {
  it('renders email input as required', () => {
    render(<WaitlistSection />)
    const emailInput = screen.getByPlaceholderText(WAITLIST.EMAIL_PLACEHOLDER)
    expect(emailInput).toBeRequired()
  })

  it('renders name input as not required', () => {
    render(<WaitlistSection />)
    const nameInput = screen.getByPlaceholderText(WAITLIST.NAME_PLACEHOLDER)
    expect(nameInput).not.toBeRequired()
  })

  it('submit button has correct label', () => {
    render(<WaitlistSection />)
    expect(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL })).toBeInTheDocument()
  })
})

describe('WaitlistSection — submit states', () => {
  it('disables submit button while submitting', async () => {
    let resolveFetch!: (v: Response) => void
    const fetchPromise = new Promise<Response>((res) => { resolveFetch = res })
    vi.spyOn(global, 'fetch').mockReturnValueOnce(fetchPromise)

    render(<WaitlistSection />)
    fireEvent.change(screen.getByPlaceholderText(WAITLIST.EMAIL_PLACEHOLDER), { target: { value: 'test@example.com' } })
    fireEvent.click(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL }))

    await waitFor(() => {
      expect(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL })).toBeDisabled()
    })

    resolveFetch(new Response(JSON.stringify({ ok: true }), { status: 200 }))
  })

  it('shows success message on 200 response', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValueOnce(
      new Response(JSON.stringify({ ok: true }), { status: 200 })
    )
    render(<WaitlistSection />)
    fireEvent.change(screen.getByPlaceholderText(WAITLIST.EMAIL_PLACEHOLDER), { target: { value: 'test@example.com' } })
    fireEvent.click(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL }))
    await waitFor(() => expect(screen.getByText(WAITLIST.SUCCESS_MESSAGE)).toBeInTheDocument())
  })

  it('shows error message on 4xx response', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValueOnce(
      new Response(JSON.stringify({ error: 'bad request' }), { status: 400 })
    )
    render(<WaitlistSection />)
    fireEvent.change(screen.getByPlaceholderText(WAITLIST.EMAIL_PLACEHOLDER), { target: { value: 'test@example.com' } })
    fireEvent.click(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL }))
    await waitFor(() => expect(screen.getByText(WAITLIST.ERROR_MESSAGE)).toBeInTheDocument())
  })

  it('fires waitlist_form_submitted with email_domain and has_name on 200', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValueOnce(
      new Response(JSON.stringify({ ok: true }), { status: 200 })
    )
    render(<WaitlistSection />)
    fireEvent.change(screen.getByPlaceholderText(WAITLIST.EMAIL_PLACEHOLDER), { target: { value: 'test@example.com' } })
    fireEvent.click(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL }))
    await waitFor(() =>
      expect(posthog.capture).toHaveBeenCalledWith('waitlist_form_submitted', {
        email_domain: 'example.com',
        has_name: false,
      })
    )
  })

  it('fires waitlist_form_error with error_type on network error', async () => {
    vi.spyOn(global, 'fetch').mockRejectedValueOnce(new Error('network'))
    render(<WaitlistSection />)
    fireEvent.change(screen.getByPlaceholderText(WAITLIST.EMAIL_PLACEHOLDER), { target: { value: 'test@example.com' } })
    fireEvent.click(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL }))
    await waitFor(() =>
      expect(posthog.capture).toHaveBeenCalledWith('waitlist_form_error', { error_type: 'network' })
    )
  })

  it('calls fetch with WAITLIST.ENDPOINT', async () => {
    const fetchSpy = vi.spyOn(global, 'fetch').mockResolvedValueOnce(
      new Response(JSON.stringify({ ok: true }), { status: 200 })
    )
    render(<WaitlistSection />)
    fireEvent.change(screen.getByPlaceholderText(WAITLIST.EMAIL_PLACEHOLDER), { target: { value: 'test@example.com' } })
    fireEvent.click(screen.getByRole('button', { name: WAITLIST.SUBMIT_LABEL }))
    await waitFor(() => expect(fetchSpy).toHaveBeenCalledWith(WAITLIST.ENDPOINT, expect.any(Object)))
  })
})
```

**File: src/components/sections/__tests__/SocialProofAndFAQ.test.tsx**

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { SOCIAL_PROOF, FAQ } from '@/lib/content'

import { SocialProofSection } from '@/components/sections/SocialProofSection'
import { FAQSection } from '@/components/sections/FAQSection'

describe('SocialProofSection', () => {
  it('renders section heading', () => {
    render(<SocialProofSection />)
    expect(screen.getByText(SOCIAL_PROOF.SECTION_HEADING)).toBeInTheDocument()
  })

  it('contains TESTIMONIAL_REQUIRED placeholder marker in HTML', () => {
    const { container } = render(<SocialProofSection />)
    expect(container.innerHTML).toContain('TESTIMONIAL_REQUIRED')
  })
})

describe('FAQSection', () => {
  it('renders all FAQ questions', () => {
    render(<FAQSection />)
    FAQ.ITEMS.forEach(({ question }) => {
      expect(screen.getByText(question)).toBeInTheDocument()
    })
  })

  it('renders FAQ answers', () => {
    render(<FAQSection />)
    FAQ.ITEMS.forEach(({ answer }) => {
      expect(screen.getByText(answer)).toBeInTheDocument()
    })
  })
})
```

**Extend: src/lib/content.test.ts**

Append to the bottom of the existing file (do NOT modify existing tests):

```typescript
// ─── COPY-01: Banned-word compliance ──────────────────────────────────────────
import { HERO, HOW_IT_WORKS, FEATURES, PRICING, WAITLIST, FAQ, SOCIAL_PROOF } from './content'

const BANNED_WORDS = [
  'AI-powered',
  'platform',
  'solution',
  'leverage',
  'seamless',
  'intuitive',
  'workflow automation',
  'generative AI',
  'storyboard software',
  'asset management',
  'collaboration hub',
  'template',
  'streamline',
] as const

function extractAllStrings(obj: unknown): string[] {
  if (typeof obj === 'string') return [obj]
  if (Array.isArray(obj)) return obj.flatMap(extractAllStrings)
  if (obj !== null && typeof obj === 'object') return Object.values(obj).flatMap(extractAllStrings)
  return []
}

const allCopyStrings = extractAllStrings({
  HERO,
  HOW_IT_WORKS,
  FEATURES,
  PRICING,
  WAITLIST,
  FAQ,
  SOCIAL_PROOF,
})

describe('COPY-01 — banned-word compliance', () => {
  BANNED_WORDS.forEach((word) => {
    it(`no copy contains "${word}"`, () => {
      const lowWord = word.toLowerCase()
      const violations = allCopyStrings.filter((s) =>
        s.toLowerCase().includes(lowWord)
      )
      expect(violations).toHaveLength(0)
    })
  })
})
```

**Run order:** After writing all files, run `cd conjure-landing-page && npm test -- --run` to confirm:
- Existing tests pass (content.test.ts 5 tests stay GREEN)
- Banned-word tests pass GREEN (content.ts was written clean)
- New section tests FAIL with "Cannot find module" or similar (RED — components don't exist yet)
  </action>
  <verify>
    <automated>cd /Users/christopheraumen/Documents/Conjure-Landing-Page/conjure-landing-page && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
Five test files created under `src/components/sections/__tests__/` and `src/lib/content.test.ts` extended. Running `npm test -- --run` shows existing tests pass, banned-word tests pass, and new section tests fail with import errors (RED state confirmed). Test count increases from 5 to 30+.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "glass-surface" conjure-landing-page/src/app/globals.css` returns ≥ 2
2. `grep -c "fade-in-section" conjure-landing-page/src/app/globals.css` returns ≥ 2
3. `ls conjure-landing-page/src/components/sections/__tests__/` shows four test files
4. `cd conjure-landing-page && npm test -- --run` — existing 5 tests pass, banned-word tests pass, section tests fail RED
</verification>

<success_criteria>
- `.glass-surface` and `.fade-in-section` utility classes exist in globals.css using existing CSS vars
- All five test files exist with the assertions described above
- `npm test -- --run` exits with some failures (RED test stubs) but all pre-existing tests remain GREEN
- No watch-mode flags used in any verify command
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-page/02-00-SUMMARY.md`
</output>
