---
phase: 04-qa-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - PERF-03

must_haves:
  truths:
    - "A curl OPTIONS preflight to conjurestudio.app/api/waitlist with Origin: https://conjurestudio.ai returns Access-Control-Allow-Origin: https://conjurestudio.ai in the response headers"
    - "A curl POST to conjurestudio.app/api/waitlist with Origin: https://conjurestudio.ai includes the CORS allow-origin header in the response"
    - "The browser does not block the waitlist form POST from conjurestudio.ai due to a CORS error"
  artifacts:
    - path: "conjurestudio.app Conjure app repo — /api/waitlist route handler"
      provides: "Access-Control-Allow-Origin response header for https://conjurestudio.ai origin"
      contains: "Access-Control-Allow-Origin: https://conjurestudio.ai"
  key_links:
    - from: "conjurestudio.ai waitlist form"
      to: "conjurestudio.app/api/waitlist"
      via: "cross-origin fetch with Content-Type: application/json"
      pattern: "Access-Control-Allow-Origin"
---

<objective>
Add CORS headers to the conjurestudio.app waitlist API so the production landing page can POST to it without a browser CORS block.

Purpose: The waitlist form (WAIT-02) makes a direct cross-origin fetch from https://conjurestudio.ai to https://conjurestudio.app/api/waitlist. Without the correct response headers, every browser blocks this request before it reaches the server. This is the only outstanding launch blocker.

Output: conjurestudio.app/api/waitlist returns Access-Control-Allow-Origin: https://conjurestudio.ai on both OPTIONS preflight and POST requests. Verified with curl before proceeding to the QA checklist in Plan 02.
</objective>

<execution_context>
@/Users/christopheraumen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopheraumen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Add CORS headers to conjurestudio.app/api/waitlist</name>
  <files>conjurestudio.app repo — api/waitlist route handler (e.g. app/api/waitlist/route.ts for Next.js App Router)</files>
  <action>
In the Conjure app repo (conjurestudio.app — separate repo, not this one), locate the /api/waitlist route handler and add the following CORS response headers to every response it returns:

  Access-Control-Allow-Origin: https://conjurestudio.ai
  Access-Control-Allow-Methods: POST, OPTIONS
  Access-Control-Allow-Headers: Content-Type

Additionally, handle the OPTIONS preflight request explicitly — when the HTTP method is OPTIONS, return HTTP 200 (or 204) immediately with those three headers and no body. The browser sends this preflight before the POST because the Content-Type is application/json (a non-simple request).

The allowed origin MUST be exactly https://conjurestudio.ai — no trailing slash, no wildcard, no localhost. This is a locked decision.

Deploy the change to production (conjurestudio.app) before continuing.
  </action>
  <verify>
    <automated>HUMAN-ACTION — user deploys change to conjurestudio.app; no automated verify applicable before deploy</automated>
  </verify>
  <done>CORS headers added to /api/waitlist route and deployed to production conjurestudio.app.</done>
  <resume-signal>Type "deployed" once the change is live on conjurestudio.app</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify CORS headers with curl</name>
  <files>(no files modified — verification only)</files>
  <action>
Run both curl commands below against the live conjurestudio.app API. Both must pass before this plan is considered complete.

Preflight check (OPTIONS):
  curl -I -X OPTIONS https://conjurestudio.app/api/waitlist \
    -H "Origin: https://conjurestudio.ai" \
    -H "Access-Control-Request-Method: POST" \
    -H "Access-Control-Request-Headers: Content-Type"

Expected: response headers include "Access-Control-Allow-Origin: https://conjurestudio.ai"

POST check (with origin):
  curl -X POST https://conjurestudio.app/api/waitlist \
    -H "Origin: https://conjurestudio.ai" \
    -H "Content-Type: application/json" \
    -d '{"email":"qa-cors-verify@example.com","name":"CORS QA"}' \
    -v 2>&1 | grep -i "access-control"

Expected: output line containing "< Access-Control-Allow-Origin: https://conjurestudio.ai"

If either check fails: the OPTIONS preflight may not be handled (look for 404 or 405 on the first curl), or the header may be absent on POST responses. Fix before proceeding to Plan 02.

Also confirm the existing test suite is still green:
  cd /Users/christopheraumen/Documents/Conjure-Landing-Page/conjure-landing-page && npx vitest run 2>&1 | tail -10
(No landing page code changed — this confirms the baseline.)
  </action>
  <verify>
    <automated>curl -I -X OPTIONS https://conjurestudio.app/api/waitlist -H "Origin: https://conjurestudio.ai" -H "Access-Control-Request-Method: POST" -H "Access-Control-Request-Headers: Content-Type" 2>&1 | grep -i "access-control-allow-origin"</automated>
  </verify>
  <done>Both curl checks return "Access-Control-Allow-Origin: https://conjurestudio.ai" in their response headers. Test suite shows all 77 tests passing.</done>
</task>

</tasks>

<verification>
Run both curl commands from Task 2 and confirm output contains "Access-Control-Allow-Origin: https://conjurestudio.ai".

Run the landing page test suite: cd /Users/christopheraumen/Documents/Conjure-Landing-Page/conjure-landing-page && npx vitest run 2>&1 | tail -10 — must show all tests passing.
</verification>

<success_criteria>
- curl OPTIONS preflight returns Access-Control-Allow-Origin: https://conjurestudio.ai
- curl POST with Origin: https://conjurestudio.ai returns Access-Control-Allow-Origin: https://conjurestudio.ai
- Landing page test suite: 77 tests, all passing
- Plan 02 (QA checklist) is now unblocked — the waitlist form end-to-end test can run from the production domain
</success_criteria>

<output>
After completion, create `.planning/phases/04-qa-and-launch/04-01-SUMMARY.md` documenting:
- CORS fix deployed (date, what was changed)
- curl verification output (both checks)
- Any issues encountered and how resolved
</output>
