---
phase: 03-admin-route
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - conjure-landing-page/src/lib/session.test.ts
  - conjure-landing-page/src/app/admin/page.test.ts
  - conjure-landing-page/src/app/admin/login/actions.test.ts
  - conjure-landing-page/src/lib/supabase-admin.test.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05

must_haves:
  truths:
    - "Unauthenticated GET /admin redirects to /admin/login (proxy layer)"
    - "Unauthenticated render of AdminPage redirects to /admin/login (Server Component layer — CVE mitigation)"
    - "Correct password sets httpOnly admin_session cookie and redirects to /admin"
    - "Incorrect password returns error state and sets no cookie"
    - "Admin page query orders by created_at ascending: false and shows total count"
    - "SUPABASE_SERVICE_ROLE_KEY cannot enter the client bundle (compile-time import guard)"
  artifacts:
    - path: "conjure-landing-page/src/lib/session.test.ts"
      provides: "RED stubs for encrypt/decrypt/createSession/deleteSession (ADMIN-01)"
    - path: "conjure-landing-page/src/app/admin/page.test.ts"
      provides: "RED stubs for unauthenticated redirect (ADMIN-02), waitlist query (ADMIN-04, ADMIN-05)"
    - path: "conjure-landing-page/src/app/admin/login/actions.test.ts"
      provides: "RED stubs for login success and failure (ADMIN-03)"
    - path: "conjure-landing-page/src/lib/supabase-admin.test.ts"
      provides: "RED stub for server-only import guard (ADMIN-04)"
  key_links:
    - from: "session.test.ts"
      to: "src/lib/session.ts"
      via: "dynamic import — file does not exist yet so test must fail"
      pattern: "import.*@/lib/session"
    - from: "page.test.ts"
      to: "src/app/admin/page.tsx"
      via: "fs.readFileSync source scan — file does not exist yet"
      pattern: "redirect.*admin/login"
    - from: "actions.test.ts"
      to: "src/app/admin/login/actions.ts"
      via: "dynamic import — file does not exist yet"
      pattern: "import.*actions"
---

<objective>
Install the three new production dependencies required for Phase 3, then write RED failing tests for all five ADMIN requirements. Tests must fail because the implementation files do not exist yet. This is the TDD Wave 0 — tests define the contract before any source code is written.

Purpose: Establish the test contract for the defense-in-depth admin auth pattern. Tests remain RED until Plan 02 implements session.ts, supabase-admin.ts, proxy.ts, and the admin route files.
Output: 3 new packages in package.json + 4 RED test files covering ADMIN-01 through ADMIN-05.
</objective>

<execution_context>
@/Users/christopheraumen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopheraumen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-admin-route/03-RESEARCH.md
@.planning/phases/03-admin-route/03-VALIDATION.md

@conjure-landing-page/package.json
@conjure-landing-page/vitest.config.ts
@conjure-landing-page/src/lib/env.ts
@conjure-landing-page/src/lib/env.test.ts
@conjure-landing-page/src/lib/glass.test.ts
@conjure-landing-page/src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install jose, @supabase/supabase-js, server-only</name>
  <files>conjure-landing-page/package.json, conjure-landing-page/package-lock.json</files>
  <action>
    Run from the conjure-landing-page/ directory:

    ```
    cd conjure-landing-page && npm install jose @supabase/supabase-js server-only
    ```

    - `jose` is a production dep (used in session.ts which ships in the server bundle)
    - `@supabase/supabase-js` is a production dep (used in supabase-admin.ts)
    - `server-only` is a production dep (its import side-effect runs at build time)

    All three install as production dependencies (no --save-dev). Do not add them to devDependencies.

    After install, verify package.json lists all three under "dependencies":
    - `"jose": "^5.x"` (latest 5.x)
    - `"@supabase/supabase-js": "^2.x"` (latest 2.x)
    - `"server-only": "latest"` (tiny package — no version pinning required)
  </action>
  <verify>
    <automated>cd conjure-landing-page && node -e "require('./node_modules/jose/package.json'); require('./node_modules/@supabase/supabase-js/package.json'); require('./node_modules/server-only/package.json'); console.log('all three installed')"</automated>
  </verify>
  <done>package.json "dependencies" block lists jose, @supabase/supabase-js, and server-only. node_modules contains all three packages. npm install exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for ADMIN-01 through ADMIN-05</name>
  <files>
    conjure-landing-page/src/lib/session.test.ts,
    conjure-landing-page/src/app/admin/page.test.ts,
    conjure-landing-page/src/app/admin/login/actions.test.ts,
    conjure-landing-page/src/lib/supabase-admin.test.ts
  </files>
  <behavior>
    ADMIN-01: session.ts exports encrypt and decrypt; decrypt(undefined) returns null; decrypt(validToken) returns { authenticated: true }; createSession and deleteSession are exported functions
    ADMIN-01 (CVE): admin/page.tsx source contains `redirect('/admin/login')` AND `await decrypt(` — both must be present for defense-in-depth
    ADMIN-02: proxy.ts (or src/proxy.ts) source contains `startsWith('/admin')` AND `redirect` AND `admin_session` — confirms proxy matcher and redirect logic
    ADMIN-03a: login('correct') calls createSession once and then calls redirect('/admin')
    ADMIN-03b: login('wrong') returns { error: 'Incorrect password' } and does NOT call createSession
    ADMIN-04: supabase-admin.ts source contains `import 'server-only'` at the top — compile-time guard
    ADMIN-04: supabase-admin.ts source contains `persistSession: false` and `autoRefreshToken: false`
    ADMIN-05: admin/page.tsx source contains `ascending: false` and the total count pattern (e.g., `.length`)
  </behavior>
  <action>
    Create four test files using the source-scan pattern established in glass.test.ts and env.test.ts.

    **Test strategy:** Because session.ts, proxy.ts, supabase-admin.ts, and admin/page.tsx do not exist yet, tests that import or scan them will throw ENOENT or import errors. That is the intended RED state. Do not create any implementation files in this task.

    --- src/lib/session.test.ts ---

    Use `import { fileURLToPath } from 'url'` + `path.dirname` for __dirname (ESM pattern, matching glass.test.ts).

    Tests:
    1. "ADMIN-01: session.ts exports encrypt, decrypt, createSession, deleteSession" — `import` from '@/lib/session' and assert all four are functions. This will throw MODULE_NOT_FOUND in RED state.
    2. "ADMIN-01: decrypt(undefined) returns null" — call decrypt(undefined), expect null.
    3. "ADMIN-01: decrypt(validToken) returns { authenticated: true }" — call encrypt({ authenticated: true }), then decrypt the result, expect { authenticated: true }. Use `process.env.SESSION_SECRET = 'test-secret-that-is-at-least-32-bytes-long'` in beforeAll for this test only.

    --- src/lib/supabase-admin.test.ts ---

    Source-scan pattern (fs.readFileSync).

    Tests:
    1. "ADMIN-04: supabase-admin.ts contains import 'server-only' at top" — read src/lib/supabase-admin.ts, assert it contains `import 'server-only'`. Will throw ENOENT in RED state.
    2. "ADMIN-04: supabase-admin.ts disables Supabase auth persistence" — same file read, assert `persistSession: false` and `autoRefreshToken: false` both present.
    3. "ADMIN-04: supabase-admin.ts does not export SUPABASE_SERVICE_ROLE_KEY or SUPABASE_URL" — assert source does NOT contain `export.*SUPABASE_SERVICE_ROLE_KEY` as a string literal export.

    --- src/app/admin/page.test.ts ---

    Source-scan pattern. Read `src/app/admin/page.tsx`.

    Tests:
    1. "ADMIN-02: admin/page.tsx re-verifies session via decrypt() — CVE-2025-29927 mitigation" — assert source contains `await decrypt(` and `redirect('/admin/login')`.
    2. "ADMIN-04/05: admin/page.tsx queries waitlist table ordered newest-first" — assert source contains `from('waitlist')` and `ascending: false`.
    3. "ADMIN-05: admin/page.tsx shows total signup count" — assert source contains `.length` (total count computed from results array).
    4. "ADMIN-02: proxy.ts redirects /admin requests without valid session cookie" — read `proxy.ts` (project root — `path.resolve(__dirname, '../../../proxy.ts')` from the test file location inside src/app/admin/), assert source contains `startsWith('/admin')`, `admin_session`, and `redirect`.

    NOTE on proxy.ts path: The test file is at `src/app/admin/page.test.ts`. The proxy.ts is at project root `conjure-landing-page/proxy.ts`. Use:
    ```typescript
    const proxyPath = path.resolve(__dirname, '../../../../proxy.ts')
    ```
    (four levels up from src/app/admin/ to reach conjure-landing-page/)

    --- src/app/admin/login/actions.test.ts ---

    Import-based test using vi.mock.

    Tests:
    1. "ADMIN-03: login() with correct password calls createSession and redirects to /admin" — mock `next/navigation` redirect, mock `@/lib/session` createSession, set `process.env.ADMIN_PASSWORD = 'test-password'`, call login(undefined, formData with password='test-password'), assert createSession was called once.
    2. "ADMIN-03: login() with wrong password returns error state, never calls createSession" — same setup, formData with password='wrong', assert return value has `error` property, assert createSession NOT called.

    Note: `redirect()` in Next.js throws a NEXT_REDIRECT error internally. Mock it as `vi.fn()` that does nothing (so the test function returns rather than throwing). Use `vi.mock('next/navigation', () => ({ redirect: vi.fn() }))`.

    Also mock `@/lib/session`: `vi.mock('@/lib/session', () => ({ createSession: vi.fn().mockResolvedValue(undefined) }))`.

    After writing all four files, run the test suite to CONFIRM they fail:
    ```
    cd conjure-landing-page && npx vitest run src/lib/session.test.ts src/lib/supabase-admin.test.ts src/app/admin/page.test.ts src/app/admin/login/actions.test.ts --reporter=verbose 2>&1 | tail -30
    ```
    Expected: all tests in these 4 files fail (RED). The rest of the suite should still pass. If any of the new tests accidentally pass, the stub is wrong — add a `expect.fail('implementation not yet created')` guard.
  </action>
  <verify>
    <automated>cd conjure-landing-page && npx vitest run src/lib/session.test.ts src/lib/supabase-admin.test.ts src/app/admin/page.test.ts src/app/admin/login/actions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test files exist. Running the targeted test command shows failures (RED). Running the full suite (npx vitest run) shows the existing tests still pass and the 4 new files show failures. No accidental green tests in the new files.</done>
</task>

</tasks>

<verification>
1. `cd conjure-landing-page && node -e "require('./node_modules/jose/package.json'); require('./node_modules/@supabase/supabase-js/package.json'); require('./node_modules/server-only/package.json'); console.log('all installed')"` — exits 0
2. All 4 test files exist under `conjure-landing-page/src/`
3. `cd conjure-landing-page && npx vitest run --reporter=verbose 2>&1 | tail -30` — existing tests green, new 4 files RED
</verification>

<success_criteria>
- jose, @supabase/supabase-js, server-only in package.json "dependencies"
- 4 test files written with RED stubs covering ADMIN-01 through ADMIN-05
- Full suite: pre-existing tests pass, Phase 3 stubs fail (intended RED state)
- No implementation files created in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-route/03-01-SUMMARY.md` following the summary template at `@/Users/christopheraumen/.claude/get-shit-done/templates/summary.md`.
</output>
